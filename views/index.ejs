<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h3>All Chats </h3>
    <form method="GET" action="/chats/new">
        <button>New Chat</button>
    </form>
    <br>
    <% for(chat of chats){ %>
        <div class="chat">
            <p>From : <b><%= chat.from %></b></p>
            <div>
                <p class="msg"><%= chat.msg %></p>              
                    <form action="/chats/<%=chat._id%>/edit" method="get">
                        <button>Edit Chat</button>
                    </form>
            </div>
            <p>Received by <b><%= chat.to %></b></p>
            <br>
            <p><%=chat.created_at.toString().split(" ")[4]%></p>
            <p><%=chat.created_at.toString().split(" ")[2]%>&nbsp;<%=chat.created_at.toString().split(" ")[1]%>&nbsp;<%=chat.created_at.toString().split(" ")[3]%></p>
            <form action="/chats/<%=chat._id%>?_method=DELETE" method="POST">
                <button>Delete</button>
            </form>
        </div>
        <br>
        <br>
        <%} %>
</body>
</html>

